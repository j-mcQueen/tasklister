export default function Login() {
    const openForms = () => {
    
    }

    return (
        <div id="wrap">
            <button
                type="button"
                id="log"
                onClick={openForms}
            >
                login
            </button>
        </div>
    )
}